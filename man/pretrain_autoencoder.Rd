% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretrain.R
\name{pretrain_autoencoder}
\alias{pretrain_autoencoder}
\alias{pretrain_denoising}
\title{Pre-train the encoder via batch denoising autoencoder}
\usage{
pretrain_autoencoder(
  peak_params,
  H,
  W,
  n_synthetic = 5000L,
  epochs = 50L,
  batch_size = 32L,
  lr = 0.001,
  weight_decay = 1e-04,
  add_noise = TRUE,
  stem_stride_rt = 4L,
  stem_stride_cv = 1L,
  device = if (torch::cuda_is_available()) "cuda" else "cpu",
  save_path = NULL,
  seed = 42L
)

pretrain_denoising(
  peak_params,
  H,
  W,
  n_synthetic = 5000L,
  epochs = 50L,
  batch_size = 32L,
  lr = 0.001,
  weight_decay = 1e-04,
  add_noise = TRUE,
  stem_stride_rt = 4L,
  stem_stride_cv = 1L,
  device = if (torch::cuda_is_available()) "cuda" else "cpu",
  save_path = NULL,
  seed = 42L
)
}
\arguments{
\item{peak_params}{Output of \code{\link[=estimate_peak_params]{estimate_peak_params()}}.}

\item{H, W}{Synthetic image dimensions (must match downstream input).}

\item{n_synthetic}{Number of synthetic images.}

\item{epochs, batch_size, lr, weight_decay}{Optimization parameters.}

\item{add_noise}{If TRUE, denoising AE; if FALSE, reconstruction AE.}

\item{stem_stride_rt, stem_stride_cv}{Stem stride for the encoder.}

\item{device}{"cpu" or "cuda".}

\item{save_path}{If non-NULL, writes encoder and full autoencoder to disk.}

\item{seed}{RNG seed.}
}
\value{
List with \code{encoder}, \code{autoencoder}, and \code{loss_history}.
}
\description{
Generates \code{n_synthetic} synthetic GC-DMS images, trains a denoising
autoencoder on them (noisy -> clean), and returns the trained encoder.
Memory-limited: the full synthetic dataset is held in RAM. For larger
totals, use \code{\link[=pretrain_denoising_online]{pretrain_denoising_online()}}.
}
\seealso{
\code{\link[=pretrain_denoising_online]{pretrain_denoising_online()}}
}

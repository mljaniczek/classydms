% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{cv_pretrained_oof}
\alias{cv_pretrained_oof}
\title{5-fold CV classification with a pre-trained encoder}
\usage{
cv_pretrained_oof(
  Z_list,
  y,
  encoder,
  k = 5L,
  epochs = 20L,
  batch_size = 4L,
  lr = 0.001,
  weight_decay = 1e-04,
  dropout_p = 0.3,
  freeze_backbone = TRUE,
  unfreeze_last_block = TRUE,
  augment = TRUE,
  rt_shift = 25L,
  cv_shift = 5L,
  intensity_scale = c(0.9, 1.1),
  noise_sd = 0,
  seed = 1L
)
}
\arguments{
\item{Z_list}{List of preprocessed, padded intensity matrices.}

\item{y}{Binary labels (integer or factor, two unique values).}

\item{encoder}{A pre-trained \code{dms_encoder}.}

\item{k}{Number of CV folds (default 5).}

\item{epochs}{Number of training epochs per fold.}

\item{batch_size}{Mini-batch size.}

\item{lr}{Adam learning rate.}

\item{weight_decay}{Adam L2 weight decay.}

\item{dropout_p}{Dropout probability before the linear classifier.}

\item{freeze_backbone}{If TRUE, encoder weights are not updated
during fine-tuning.}

\item{unfreeze_last_block}{If TRUE, the last encoder residual block
(\code{layer3}) is set trainable even when \code{freeze_backbone = TRUE}.}

\item{augment}{If TRUE, apply training-time augmentation
(\code{\link[=augment_batch]{augment_batch()}}: random RT/CV shifts, intensity scaling, noise).}

\item{rt_shift, cv_shift}{Maximum augmentation shift magnitudes
(pixels) along RT and CV.}

\item{intensity_scale}{Length-2 numeric vector giving the bounds of
random intensity rescaling.}

\item{noise_sd}{SD of Gaussian noise added to augmented images
(0 disables).}

\item{seed}{RNG seed for fold assignment and training.}
}
\value{
List with \code{probs} (out-of-fold predictions) and \code{auc}.
}
\description{
Out-of-fold predictions are aggregated into a single AUC. Each fold
independently deep-clones the encoder via
\code{\link[=build_pretrained_classifier]{build_pretrained_classifier()}} to prevent cross-fold leakage.
}

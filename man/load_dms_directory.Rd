% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{load_dms_directory}
\alias{load_dms_directory}
\title{Read all GC-DMS files in a directory and assemble a samples list}
\usage{
load_dms_directory(
  disease_dir,
  control_dir,
  pattern = "POS.*\\\\.(xls|txt|tsv|csv)$",
  verbose = TRUE
)
}
\arguments{
\item{disease_dir, control_dir}{Paths to directories.}

\item{pattern}{Regex matching the data files. Default selects
positive-channel files; change to \code{"NEG.*\\\\.(xls|txt|tsv|csv)$"}
for negative-channel-only.}

\item{verbose}{Whether to print progress messages.}
}
\value{
List with \code{samples} (named list of sample lists) and \code{y}
(named integer vector with 2 = disease, 1 = control).
}
\description{
Convenience wrapper that lists files matching \code{pattern} in
\code{disease_dir} and \code{control_dir}, reads each with \code{\link[=read_dms_file]{read_dms_file()}}
(skipping any that error), assigns each sample a name based on the
file basename so downstream results can be traced back to a specific
subject, and returns a parallel pair \code{samples} and \code{y}.
}
\details{
Default \code{pattern} selects the positive ion channel only (POS files),
which is what the classification pipeline trains on. The negative
channel is not used.

Sample names are set to the file basename with extension stripped
(e.g., \code{"241115-S1994-S1-DMS1_POS"}). This means you can index a
sample by name later --- e.g., \code{samples[["241115-S1994-S1-DMS1_POS"]]}
--- and so can \code{y} if you set \code{names(y) <- names(samples)}. The
function explicitly verifies that no name is the empty string,
which would otherwise propagate through \code{lapply()} and cause
"attempt to use zero-length variable name" errors downstream.
}

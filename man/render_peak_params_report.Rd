% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports.R
\name{render_peak_params_report}
\alias{render_peak_params_report}
\title{Render the peak-parameters diagnostic report}
\usage{
render_peak_params_report(
  peak_params,
  y = NULL,
  batch = NULL,
  cohort_name = "cohort",
  output_file = "peak_params_report.html",
  output_dir = NULL,
  quiet = TRUE
)
}
\arguments{
\item{peak_params}{Output of \code{\link[=estimate_peak_params]{estimate_peak_params()}}, containing the
\code{sample_idx_raw} and \code{sample_names} fields.}

\item{y}{Optional named integer or factor vector of class labels
(e.g. \verb{2 = disease, 1 = control}). Names must match
\code{peak_params$sample_names}. When \code{NULL}, class-comparison sections
are skipped and the report is rendered label-agnostic.}

\item{batch}{Optional named vector (character or factor) of batch
labels used to color the sample PCA. Names must match
\code{peak_params$sample_names}. \code{NULL} skips batch coloring.}

\item{cohort_name}{Short label displayed in the report title
(e.g. \code{"CHF"} or \code{"AKI"}).}

\item{output_file}{Output HTML filename.}

\item{output_dir}{Directory to render into. Defaults to the directory
part of \code{output_file}; created if missing.}

\item{quiet}{Whether to suppress rmarkdown chatter (default \code{TRUE}).}
}
\value{
Path to the rendered HTML file (invisibly).
}
\description{
Renders an HTML report summarizing the output of
\code{\link[=estimate_peak_params]{estimate_peak_params()}}, including per-sample peak counts, spatial
hotspot maps, sample-level clustering and shift analysis with
before-and-after global-alignment visualization, and per-class
distributions of peak widths, intensities, and background noise. When
class labels are supplied, class comparisons are reported alongside
Benjamini-Hochberg-adjusted p-values.
}
\details{
\code{peak_params} must contain the \code{sample_idx_raw} field (parallel to
\code{rt_loc_raw}), which was added in classydms after commit
\verb{Add sample_idx_raw and sample_names to estimate_peak_params}. Older
outputs of \code{\link[=estimate_peak_params]{estimate_peak_params()}} will be rejected with an
informative error.

Deferred to a follow-up (not covered here): k-means clustering on
per-sample shift vectors to detect unlabeled batch structure; anchor
peak analysis; sporadic-peak analysis; volcano plot; and the
downstream \code{build_peak_catalog()} function that this report is
designed to inform.
}

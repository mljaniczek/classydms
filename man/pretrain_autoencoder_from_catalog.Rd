% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretrain.R
\name{pretrain_autoencoder_from_catalog}
\alias{pretrain_autoencoder_from_catalog}
\title{Pre-train the encoder from a peak catalog}
\usage{
pretrain_autoencoder_from_catalog(
  catalog,
  H,
  W,
  n_synthetic = 2000L,
  epochs = 30L,
  batch_size = 32L,
  lr = 0.001,
  weight_decay = 1e-04,
  add_noise = TRUE,
  stem_stride_rt = 4L,
  stem_stride_cv = 1L,
  device = if (torch::cuda_is_available()) "cuda" else "cpu",
  seed = 42L
)
}
\arguments{
\item{catalog}{A \code{peak_catalog} object.}

\item{H, W}{Synthetic image dimensions.}

\item{n_synthetic}{Number of synthetic samples to generate.}

\item{epochs, batch_size, lr, weight_decay}{Optimization parameters.}

\item{add_noise}{If TRUE, denoising autoencoder; if FALSE,
reconstruction.}

\item{stem_stride_rt, stem_stride_cv}{Encoder stem strides.}

\item{device}{"cpu" or "cuda".}

\item{seed}{RNG seed.}
}
\value{
List with \code{encoder}, \code{autoencoder}, \code{loss_history}.
}
\description{
Same idea as \code{\link[=pretrain_autoencoder]{pretrain_autoencoder()}} but generates the synthetic
pretraining set via \code{\link[=generate_one_synthetic_from_catalog]{generate_one_synthetic_from_catalog()}}. Use
this when you have a catalog (perhaps loaded from disk) but not the
original \code{peak_params} object.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpretability.R
\name{aggregate_saliency_masked}
\alias{aggregate_saliency_masked}
\title{Aggregate saliency maps across samples, masking padded regions}
\usage{
aggregate_saliency_masked(
  encoder,
  Z_list,
  coefs,
  sample_indices,
  orig_dims,
  device = "cpu"
)
}
\arguments{
\item{encoder}{Pre-trained \code{dms_encoder}.}

\item{Z_list}{List of padded matrices.}

\item{coefs}{Elastic net coefficients (length 64).}

\item{sample_indices}{Which samples to aggregate over.}

\item{orig_dims}{List of \code{c(H, W)} pairs giving each sample's
pre-padding dimensions.}

\item{device}{"cpu" or "cuda".}
}
\value{
Matrix of the same shape as the padded image.
}
\description{
Computes a single saliency map averaged over \code{sample_indices}. For
each sample, only the original (non-padded) region contributes; the
divisor at each pixel is the number of samples that actually had data
there. Prevents data/padding boundary artifacts from dominating the
aggregate map.
}

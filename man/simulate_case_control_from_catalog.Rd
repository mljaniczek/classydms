% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_generation.R
\name{simulate_case_control_from_catalog}
\alias{simulate_case_control_from_catalog}
\title{Simulate a case / control cohort from a peak catalog}
\usage{
simulate_case_control_from_catalog(
  catalog,
  biomarkers,
  n_cases,
  n_controls,
  H,
  W,
  size_jitter = 0.15,
  location_jitter_rt = 2,
  location_jitter_cv = 1,
  add_noise = TRUE,
  noise_scale = 1,
  seed = NULL
)
}
\arguments{
\item{catalog}{A \code{peak_catalog} object.}

\item{biomarkers}{A tibble from \code{\link[=build_biomarker_spec_catalog]{build_biomarker_spec_catalog()}}.}

\item{n_cases, n_controls}{Number of samples per group.}

\item{H, W}{Output image dimensions in pixels.}

\item{size_jitter, location_jitter_rt, location_jitter_cv}{Per-peak
jitter parameters, forwarded to the underlying generator.}

\item{add_noise}{Whether to add background noise.}

\item{noise_scale}{Multiplier on catalog noise SD.}

\item{seed}{Optional RNG seed.}
}
\value{
List with \code{samples}, \code{y}, \code{ground_truth} (the biomarker
spec), and \code{per_sample_firing} (logical matrix of \code{n_samples} x
\code{n_biomarkers}; whether each biomarker fired in each sample).
\code{samples} matches \code{\link[=load_dms_directory]{load_dms_directory()}}'s shape so downstream
pipeline steps work unchanged.
}
\description{
For each sample: determine group (case or control), then generate
via \code{\link[=generate_one_synthetic_from_catalog]{generate_one_synthetic_from_catalog()}} with the biomarker
spec's per-group prevalence and intensity overrides applied.
Non-biomarker compounds use the catalog's own prevalence in both
groups (background is shared). This directly matches the real-data
property that the \emph{same} compound positions appear across most
samples, only differing in whether individual compounds fired.
}
\details{
Compared to \code{\link[=simulate_case_control_cohort]{simulate_case_control_cohort()}} (the peak_params flow):
\itemize{
\item Shared background: every sample draws from the same catalog
compound positions rather than random-per-sample peak positions.
\item No protect_radius: catalog compounds are already at discrete
positions with no overlap between them.
\item Biomarker specification is by \code{compound_id}, not by random draw
from a pool.
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretrain.R
\name{pretrain_denoising_online}
\alias{pretrain_denoising_online}
\title{Pre-train the encoder via online denoising autoencoder}
\usage{
pretrain_denoising_online(
  peak_params = NULL,
  H,
  W,
  steps_per_epoch = 500L,
  epochs = 30L,
  batch_size = 32L,
  lr = 0.001,
  weight_decay = 1e-04,
  add_noise = TRUE,
  size_jitter = 0.15,
  grad_clip = 1,
  norm_clamp = 10,
  dust_threshold = 0.01,
  noisy_dust_threshold = NULL,
  location_mode = c("empirical", "marginal"),
  location_jitter_rt = 2,
  location_jitter_cv = 1,
  attribute_mode = c("joint", "marginal"),
  noise_scale = 1,
  catalog = NULL,
  anchor_ids = NULL,
  variable_config = NULL,
  contamination_config = NULL,
  clean_layers = NULL,
  noise_layers = NULL,
  contamination_schedule = NULL,
  mask_config = NULL,
  affine_shift_rt = 0L,
  affine_shift_cv = 0L,
  shift_group_size = 1L,
  baseline_drift_config = NULL,
  val_real = NULL,
  val_n = 200L,
  checkpoint_every = 10L,
  loss_diagnostics = TRUE,
  stem_stride_rt = 4L,
  stem_stride_cv = 1L,
  encoder_channels = c(16L, 16L, 32L, 64L, 64L),
  encoder_dropout = 0,
  anchor_dropout = 0,
  anchor_jitter_variability = NULL,
  affine_shift_prob = 1,
  stripe_noise_config = NULL,
  rt_warp_config = NULL,
  variable_source_peak_params = NULL,
  peak_shape = c("gaussian", "emg", "mix"),
  emg_tau_range = c(0.2, 0.8),
  emg_mix_prob = 0.3,
  device = if (torch::cuda_is_available()) "cuda" else "cpu",
  num_threads = NULL,
  num_workers = 1L,
  save_path = NULL,
  resume_from = NULL,
  seed = 42L,
  verbose = TRUE
)
}
\arguments{
\item{peak_params}{Output of \code{\link[=estimate_peak_params]{estimate_peak_params()}}.}

\item{H, W}{Synthetic image dimensions.}

\item{steps_per_epoch}{Number of training batches per epoch.}

\item{epochs}{Number of epochs.}

\item{batch_size}{Batch size.}

\item{lr, weight_decay}{Optimizer parameters.}

\item{add_noise}{If TRUE, denoising AE; if FALSE, reconstruction AE.}

\item{size_jitter}{Per-peak random log-normal size scale factor SD.}

\item{grad_clip}{Maximum L2 norm for gradient clipping (0 disables).}

\item{norm_clamp}{Maximum normalized pixel value (safety clamp).}

\item{dust_threshold}{Zero out pixels below this value in the CLEAN
target BEFORE normalization. Matches the dust thresholding
applied to real data via \code{\link[=baseline_basement]{baseline_basement()}} so synthetic and
real samples have the same sparsity profile entering the encoder.
\strong{Default \code{0.01}} — was \code{0} previously, which produced dense
synthetic Z matrices that mismatched real (baseline_basement'ed)
validation samples and pushed the encoder toward feature collapse.
Set to \code{0} to disable. If your real preprocessing uses a
different \code{basement_thr}, match it here.}

\item{noisy_dust_threshold}{Zero-out threshold for the NOISY input.
\code{NULL} (default) chooses a mode-appropriate value: in split-layer
mode (any \code{clean_layers} / \code{noise_layers} set) the noisy input
is left UNTHRESHOLDED so contamination and sensor noise remain
full-resolution corruption for the encoder to strip; in standard
mode the same \code{dust_threshold} is applied to noisy as to clean
(matches previous behavior). Pass an explicit scalar to override
either default (e.g., \code{0} to force no thresholding, or a small
positive value to keep some background floor in the noisy input).}

\item{location_mode}{Passed to \code{\link[=generate_one_synthetic]{generate_one_synthetic()}}. Default
\code{"empirical"} samples peak locations from the real cohort's
observed hotspots (\code{params$rt_loc_raw} / \code{cv_loc_raw}), producing
synthetic samples whose peaks cluster at cohort-typical
(RT, CV) coordinates rather than scattering across the image.
\code{"marginal"} is the previous behavior kept for backward compatibility.}

\item{location_jitter_rt, location_jitter_cv}{Passed to
\code{\link[=generate_one_synthetic]{generate_one_synthetic()}}. SD of per-peak location jitter.}

\item{attribute_mode}{Passed to \code{\link[=generate_one_synthetic]{generate_one_synthetic()}}. Default
\code{"joint"} couples per-peak sigma_rt / sigma_cv / intensity by
resampling the observed triple from \verb{params$*_raw}, so wider
peaks are drawn with correspondingly higher intensity (matching
the physical coupling in real GC-DMS). \code{"marginal"} reverts to
independent log-normal draws.}

\item{noise_scale}{Passed to \code{\link[=generate_one_synthetic]{generate_one_synthetic()}}. Multiplier
on the noise SD used at synthesis time. \code{1.0} (default) uses
real-cohort noise levels; larger values increase the corruption
the denoising autoencoder must learn to remove, which typically
improves encoder feature quality up to some optimum
(Vincent et al. 2008). Sweep \code{c(1, 3, 5, 10)} and pick the
largest value that doesn't degrade real-validation MSE. Accepts
a \strong{length-2 vector} \code{c(min, max)} to draw a fresh noise scale
per sample uniformly from that range — teaches the encoder to
handle varying real-instrument noise levels rather than assuming
a fixed floor. Recommended range for the "wild" recipe:
\code{c(1.0, 3.0)}.}

\item{anchor_ids}{Optional integer vector of catalog \code{compound_id}s.
When provided (along with \code{catalog}), synthetic samples are
generated via the three-layer noisy scheme (see
\code{\link[=generate_noisy_pretrain_sample]{generate_noisy_pretrain_sample()}}) rather than the default
catalog-prevalence firing. The anchor set fires unconditionally
in every sample; \code{variable_config} and \code{contamination_config}
optionally add per-sample layers on top.}

\item{variable_config}{Optional list; see
\code{\link[=generate_noisy_pretrain_sample]{generate_noisy_pretrain_sample()}}. Requires \code{anchor_ids}. Adds
a per-sample random subset of low-prevalence catalog compounds.}

\item{contamination_config}{Optional list; see
\code{\link[=generate_noisy_pretrain_sample]{generate_noisy_pretrain_sample()}}. Requires \code{anchor_ids}. Adds
per-sample uniform-random peaks at positions outside catalog
compound exclusion boxes.}

\item{clean_layers, noise_layers}{Optional character vectors naming
which of the three catalog layers (\code{"anchors"}, \code{"variable"},
\code{"contamination"}) go into the CLEAN target vs. only into the
NOISY input. Default \code{NULL / NULL} uses the returned
\code{pair$clean} and \code{pair$noisy} directly, which is the current
behavior where all three layers are in the clean target and only
sensor noise (from \code{noise_scale}) is corruption. Setting either
switches to split-layer denoising: \verb{Z_clean = sum of clean_layers}
and \verb{Z_noisy = Z_clean + sum of noise_layers + sensor_noise}.
Recommended recipe for skin GC-DMS pretraining:
\code{clean_layers = c("anchors", "variable")},
\code{noise_layers = c("contamination")}, plus a stronger
\code{contamination_config} (say 30–100 peaks, intensity 0.3–1.0) and
\code{noise_scale} bumped ~2×. This teaches the encoder to strip
apparatus contamination and unfamiliar peaks while preserving
the catalog compound structure — the actual denoising objective
the training loss is measuring. Layers can't appear in both
vectors; naming a layer whose config is \code{NULL} produces a warning.}

\item{contamination_schedule}{Optional named list of regime weights
giving stochastic per-sample variation in contamination level.
Currently uses fixed scale multipliers: \verb{clean = 0x},
\verb{light = 0.3x}, \verb{heavy = 1x} — where the multiplier scales BOTH
\code{contamination_config$n_per_sample} and \code{intensity_range}. Weights
normalize automatically. Example:
\code{contamination_schedule = list(clean = 0.3, light = 0.4, heavy = 0.3)}
makes 30\% of samples clean, 40\% lightly contaminated, 30\% heavily
contaminated. Prevents the encoder from overfitting to
"always messy" — real skin GC-DMS ranges from clean to messy per
subject, and the encoder should generalize to both extremes.
Requires \code{contamination_config} (the config to scale) and
\code{anchor_ids} (the three-layer path). \code{NULL} (default) disables
scheduling — every sample uses the full \code{contamination_config}.}

\item{mask_config}{Optional list enabling MAE-style masking
augmentation. When provided, zeros out random rectangles from
the NOISY input only (target unchanged), forcing the encoder to
reconstruct the masked regions from surrounding context. Fields:
\code{n_rects = c(min, max)} (default \code{c(3L, 5L)}) — how many
rectangles per sample, drawn uniformly; \code{size_frac = c(min, max)}
(default \code{c(0.05, 0.15)}) — each rectangle's area as a fraction
of image size, drawn uniformly per rectangle. Aspect ratio is
randomized per rectangle in \verb{[0.5, 2.0]}. \code{NULL} (default)
disables masking. Regularizes against synthetic-pixel-pattern
memorization; effective on top of split-layer denoising.}

\item{affine_shift_rt, affine_shift_cv}{Coherent per-sample affine
shift augmentation, in pixels. Each accepts either shape:
\itemize{
\item Scalar \code{M} (integer, default \code{0L}): fixed maximum magnitude.
Per-sample shift \code{dr ~ U(-M, +M)} (or \code{dc} for CV).
\item Length-2 vector \code{c(M_lo, M_hi)}: stochastic magnitude. Each
sample first draws a per-sample max \code{M ~ U(M_lo, M_hi)}, then
\code{dr ~ U(-M, +M)}. Models real cohorts where drift magnitude
itself varies session-to-session.
}

Applied to BOTH clean and noisy identically (data augmentation,
not drift correction). Reasonable values on 1400 x 90 images:
\verb{affine_shift_rt = 5L, affine_shift_cv = 1L} (fixed) or
\verb{affine_shift_rt = c(5L, 25L), affine_shift_cv = c(1L, 5L)}
(stochastic).}

\item{shift_group_size}{Group size for the coherent shift draw.
Accepts either shape:
\itemize{
\item Scalar \code{N} (integer, default \code{1L}): fixed. The first N samples
in each batch share ONE shift draw (magnitude, direction, and
Bernoulli activation all coherent within the group), the next
N share another, etc. \code{1L} reproduces the current
per-sample-independent behavior.
\item Length-2 vector \code{c(N_lo, N_hi)}: each successive group draws
its own size from \code{Uniform(N_lo, N_hi)}, so group boundaries
themselves vary within a batch. Matches real acquisitions
where sessions differ in length.
}

Setting scalar to \code{batch_size} models each batch as one
"acquisition session" in which every sample drifted the same
way. Also gates whether stripe patterns are grouped, when
\code{stripe_noise_config$grouped = TRUE}.}

\item{baseline_drift_config}{Optional list controlling smooth
low-frequency baseline drift added to the NOISY input only
(clean target untouched — this is corruption the encoder must
strip, teaching it to ignore residual baseline that AsLS didn't
fully remove). Fields:
\itemize{
\item \code{prob} (default \code{0.3}) — Bernoulli fraction of samples that
get baseline drift.
\item \code{direction} (\code{"rt"}, \code{"cv"}, or \code{"both"}, default \code{"rt"}) —
axis of variation. \code{"both"} adds an outer sum of two 1D drifts.
May be a subset e.g. \code{c("rt", "cv")} — one sampled per sample.
\item \code{shape} (\code{"polynomial"} or \code{"sinusoid"}, default
\code{"polynomial"}) — smooth 1D function family.
\item \code{order} (default \code{c(2L, 4L)}) — polynomial-order range; per
sample an integer is drawn from this range (ignored for
sinusoid).
\item \code{amplitude} (default \code{c(0.02, 0.10)}) — peak-to-peak amplitude
range as a fraction of image intensity units; drawn per sample.
}}

\item{val_real}{Optional list of real preprocessed, padded Z
matrices (each of size \verb{H x W}) held out as a real-data
validation set. When provided, an additional per-epoch metric
\code{real_val_MSE} is computed as the reconstruction MSE of the
autoencoder on these real samples (target = input, no noise
added). Directly measures whether encoder features learned from
synthetic transfer to real. If \code{NULL} (default), only the
synthetic validation metric is computed.}

\item{val_n}{Number of fixed validation samples (0 disables validation).}

\item{checkpoint_every}{Save encoder checkpoint every N epochs.}

\item{loss_diagnostics}{If TRUE, also print median, p95, max batch loss.}

\item{stem_stride_rt, stem_stride_cv}{Encoder stem strides.}

\item{encoder_channels}{Length-5 integer vector controlling encoder
capacity: \code{c(stem_ch, layer1_ch, layer2_ch, layer3_ch, latent_ch)}.
The final entry is the GAP-pooled feature dimension (input width
for the downstream elastic-net classifier). Default
\code{c(16L, 16L, 32L, 64L, 64L)} matches previous behavior exactly
(no projection layer added). Wider examples:
\code{c(16L, 32L, 64L, 128L, 128L)} for 128-dim latent;
\code{c(16L, 32L, 64L, 128L, 256L)} for 256-dim latent (adds a 1x1
projection on top). Same 32x/4x RT/CV spatial downsample
regardless — only channel counts differ. Downstream classifier
and feature extractor auto-detect the latent dim from the
trained encoder, so no other args need updating.}

\item{encoder_dropout}{Channel-dropout probability for the encoder
(see \code{\link[=dms_encoder]{dms_encoder()}}). Applied via \code{nn_dropout2d} between
residual block groups — whole feature-map channels are zeroed
independently with probability \code{encoder_dropout}. Standard
regularization to reduce overfit to the synthetic distribution
and improve transferability of encoder features to real data.
Default \code{0.0} disables dropout (backward-compatible).
Recommended try-values \code{0.1} and \code{0.2} when real-validation MSE
plateaus or begins rising past a certain epoch. Decoder is left
clean — only encoder-side matters for downstream transfer.
Dropout is training-only; encoder$eval() disables it for
feature extraction and inference automatically.}

\item{anchor_dropout}{Per-sample Bernoulli drop probability applied
to the anchor set before rendering. Each sample, each anchor
compound is independently dropped with this probability;
surviving anchors fire as usual. Default \code{0.0} — all anchors
always fire (backward-compatible). Recommended \code{0.1}–\code{0.2} to
simulate real subject-to-subject compound absence.}

\item{anchor_jitter_variability}{Optional length-2 numeric range
for a per-sample scalar multiplier applied to
\code{location_jitter_rt} and \code{location_jitter_cv}. When provided,
each sample draws \code{jm ~ Uniform(low, high)} and effective
jitter for that sample is \code{location_jitter_rt * jm} on RT and
\code{location_jitter_cv * jm} on CV. Whole-sample scalar
(per-anchor variation deferred as a follow-up refactor).
Default \code{NULL} disables (fixed jitter per sample).}

\item{affine_shift_prob}{Bernoulli probability that any given
sample (or group; see \code{shift_group_size}) gets an affine shift.
Default \code{1.0} — every sample when \code{affine_shift_rt}/\code{cv} > 0.
Set to e.g. \code{0.6} to have 40\% of samples receive NO shift
(better mimics real acquisition where most runs are well-aligned
and only some drift).}

\item{stripe_noise_config}{Optional list controlling row- or
column-correlated stripe noise added to the NOISY input on a
fraction of samples. Simulates thermal / electrical acquisition
artifacts. Fields:
\itemize{
\item \code{prob} (default \code{0.2}) — Bernoulli fraction of samples that
get stripe noise this batch.
\item \code{direction} (\code{"rt"} or \code{"cv"}, default \code{"rt"}) — direction
along which the noise is 1-D correlated. \code{"rt"} gives
horizontal bands (one noise value per row, tiled across
cols); \code{"cv"} gives vertical bands.
\item \code{intensity} (default \code{c(0.05, 0.15)}) — length-2 SD range
for the 1-D noise. Drawn uniformly per sample.
\item \code{grouped} (default \code{FALSE}) — when \code{TRUE}, the stripe amplitude
AND pattern are drawn once per shift group (see
\code{shift_group_size}) and shared across all samples in that
group, so a whole "acquisition session" gets the same stripe.
Applied to noisy only (before dust threshold).
}}

\item{rt_warp_config}{Optional list controlling smooth per-sample
RT warp applied identically to clean and noisy. Simulates
column-condition drift where later peaks shift more than early
peaks. Fields:
\itemize{
\item \code{strength} (default \code{0.02}) — fractional shift at the LAST
row (\code{s * H} pixels). Linear ramp from 0 at row 1. Sign is
randomized per sample. \verb{|strength|} must be \verb{< 0.5}.
\item \code{prob} (default \code{1.0}) — Bernoulli fraction of samples that
get warped.
Nearest-neighbor row indexing (O(H) per sample).
}}

\item{variable_source_peak_params}{Optional \code{peak_params} bundle
(output of \code{\link[=estimate_peak_params]{estimate_peak_params()}}) used to render the VARIABLE
layer via \code{\link[=generate_one_synthetic]{generate_one_synthetic()}} instead of the default
catalog-clustered path. When provided, each sample's variable
layer is generated by bootstrapping real observed peaks from
this bundle — real per-peak morphology, empirical location
distribution, coupled \verb{(sigma_rt, sigma_cv, intensity)} triples.
Peak count is driven by
\code{variable_source_peak_params$n_peaks$mean/sd} (each sample draws
a count from that Normal). Bypasses \code{variable_config} entirely;
passing both errors. Recommended when you want the variable layer
to reflect real skin-VOC morphology diversity that clustering
averages out. \code{NULL} (default) uses the catalog-based path
controlled by \code{variable_config}.}

\item{peak_shape}{One of \code{"gaussian"} (default), \code{"emg"}, or
\code{"mix"}. Selects the per-peak shape family:
\itemize{
\item \code{"gaussian"} — symmetric Gaussian on both axes. Preserves
previous behavior exactly.
\item \code{"emg"} — bi-Gaussian along RT (symmetric on the left of the
peak center, tail-stretched on the right). CV stays symmetric.
Approximates the asymmetric shape of real GC peaks (sharp
leading edge, gentle trailing edge). Tau (right-side widening
factor) drawn per peak from \code{emg_tau_range}.
\item \code{"mix"} — per peak, Bernoulli(\code{emg_mix_prob}) picks EMG;
otherwise Gaussian. Gives a natural mix of symmetric and
asymmetric peaks per sample. Recommended when transitioning
from pure-Gaussian to EMG since real cohorts have both.
}}

\item{emg_tau_range}{Length-2 numeric range for the EMG tau
parameter (right-side sigma widening factor). Default
\code{c(0.2, 0.8)} corresponds to right side 20-80\% wider than left.
Drawn per peak uniformly.}

\item{emg_mix_prob}{Probability of EMG shape per peak in \code{"mix"}
mode. Default \code{0.3}.}

\item{device}{"cpu" or "cuda".}

\item{num_threads}{If non-NULL, sets the number of CPU threads
used by torch for forward/backward passes via
\code{\link[torch:threads]{torch::torch_set_num_threads()}}. Apple Silicon machines benefit
most from setting this to the number of performance cores (e.g. 4
on an M-series Pro chip with 4P+10E layout), since the efficiency
cores contribute less per thread. Default \code{NULL} leaves torch's
automatic choice in place.}

\item{num_workers}{Number of parallel workers for R-side synthetic
data generation (default \code{1L} = serial). When > 1, uses
\code{future.apply::future_lapply} with a \code{future::multisession} plan
— fresh R processes (not fork), so it side-steps the macOS
libtorch fork crash where \code{mclapply} children inherit poisoned
threadpool mutexes. Cross-platform (Linux, macOS, Windows) and
handles global-variable discovery automatically. Most useful on
GPU / MPS backends where the GPU is otherwise waiting on R
between batches. A reasonable choice on a chip with N performance
cores is \code{num_workers = N - 2} (leaving a couple cores for the
GPU's communication threads, BLAS, and the OS). Each worker task
gets an independent L'Ecuyer-CMRG substream via
\code{future.seed = TRUE} for reproducibility. Requires the \code{future}
and \code{future.apply} packages when > 1; falls back to serial with
a message if either is missing.}

\item{save_path}{If non-NULL, writes encoder, autoencoder, and manifest
to disk after every epoch (so a mid-run crash leaves recoverable
artifacts).}

\item{resume_from}{If non-NULL, path to a previously-saved encoder
(\verb{*.pt}). The function looks for the corresponding \verb{_autoencoder.pt}
and \verb{_manifest.Rdata} next to it and resumes training from
\code{last_epoch_completed + 1}, inheriting prior loss history. Useful
for a "trained 32 epochs, want 64" continuation and for picking up
after a crash. All training-affecting args (\code{H}, \code{W},
\code{batch_size}, \code{lr}, \code{weight_decay}, \code{noise_scale},
\code{size_jitter}, \code{dust_threshold}, \code{location_mode},
\code{location_jitter_rt/cv}, \code{attribute_mode}, \code{stem_stride_rt/cv},
\code{anchor_ids}, \code{variable_config}, \code{contamination_config},
\code{clean_layers}, \code{noise_layers}, \code{mask_config}, \code{affine_shift_rt/cv},
\code{shift_group_size}, \code{baseline_drift_config})
should match the original run. Values that differ trigger a
warning on load listing the mismatches — training continues with
the CURRENT-call values, but the model was trained so far under
the manifest values. To resume identically, read the manifest and
copy its \code{hyperparams} list:

\if{html}{\out{<div class="sourceCode">}}\preformatted{e <- new.env(); load("path/to/enc_manifest.Rdata", envir = e)
hp <- e$training_manifest$hyperparams
hp$epochs <- 64L                     # extend past original target
hp$noise_scale_range <- NULL         # internal-only field, drop it
do.call(pretrain_denoising_online,
  c(list(catalog = catalog,
          resume_from = "path/to/enc.pt",
          save_path   = "path/to/enc.pt"), hp))
}\if{html}{\out{</div>}}

Two housekeeping notes on the replay pattern:
\enumerate{
\item \code{noise_scale_range} is an internal-derived field on the manifest;
drop it before splicing (the original \code{noise_scale} still lives
in the same hp list, so scalar/range detection re-runs cleanly).
\item \code{epochs} and \code{save_path} are the two things you typically override
at resume time — the former to extend training past the original
target, the latter to overwrite the same encoder file.
}}

\item{seed}{RNG seed.}

\item{verbose}{Whether to print epoch progress.}
}
\value{
List with \code{encoder}, \code{autoencoder}, \code{loss_history},
\code{val_loss_history}, \code{batch_loss_stats}, and \code{total_samples}.
}
\description{
Generates fresh synthetic batches on-the-fly during training, giving
constant memory usage regardless of total samples seen. Recommended
for scaling to hundreds of thousands or millions of total samples.
}
\details{
Includes scale-up safety features: a fixed validation set held constant
across epochs for stable progress tracking, periodic encoder
checkpointing for crash recovery, per-epoch manifest save with all
hyperparameters for reproducibility, and per-epoch ETA estimate.

Total samples seen = \code{steps_per_epoch * epochs * batch_size}.
}

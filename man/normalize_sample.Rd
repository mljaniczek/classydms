% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{normalize_sample}
\alias{normalize_sample}
\title{Log-quantile normalize a single sample}
\usage{
normalize_sample(Z, q = 0.95, eps = 1e-08)
}
\arguments{
\item{Z}{A non-negative numeric matrix.}

\item{q}{Quantile used as the normalization denominator (default 0.95).}

\item{eps}{Numerical floor on the denominator (default 1e-8).}
}
\value{
A normalized matrix of the same shape as \code{Z}.
}
\description{
Applies \code{log(1 + x) / Q_q(log(1 + x))}, where \code{Q_q} is the q-th quantile
of the log-transformed image. Falls back to dividing by 1 if the quantile
is below \code{eps} or non-finite. Per-sample normalization makes intensity
comparisons robust to instrument/subject-level absolute level differences.
}

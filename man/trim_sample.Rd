% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{trim_sample}
\alias{trim_sample}
\title{Apply trim bounds to a sample (clamps to actual size if smaller)}
\usage{
trim_sample(sample, bounds)
}
\arguments{
\item{sample}{A sample list (output of \code{\link[=read_dms_file]{read_dms_file()}} or
\code{\link[=process_one_sample]{process_one_sample()}}), with elements \code{Z}, \code{time}, \code{cv}, and
optionally \code{Z_pretrim}.}

\item{bounds}{A bounding box from \code{\link[=trim_bounds_from_occupancy]{trim_bounds_from_occupancy()}} or
\code{\link[=pooled_trim_bounds]{pooled_trim_bounds()}}, with \code{rt_start}, \code{rt_end}, \code{cv_start},
\code{cv_end}.}
}
\value{
The sample with its \code{Z} (and \code{Z_pretrim}, if present) trimmed
to \code{bounds}, along with the corresponding subset of the \code{time} and
\code{cv} axes. Indices are clamped to the actual matrix dimensions so a
sample smaller than the trim bounds is not an error.
}
\description{
Apply trim bounds to a sample (clamps to actual size if smaller)
}

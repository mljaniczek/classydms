% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoencoder.R
\name{dms_denoising_autoencoder}
\alias{dms_denoising_autoencoder}
\title{Composed denoising autoencoder (encoder + decoder)}
\usage{
dms_denoising_autoencoder(
  target_H,
  target_W,
  in_channels = 1L,
  stem_stride_rt = 4L,
  stem_stride_cv = 1L,
  encoder_channels = c(16L, 16L, 32L, 64L, 64L),
  dropout_p = 0
)
}
\arguments{
\item{target_H, target_W}{Input image dimensions.}

\item{in_channels}{Input channels (default 1).}

\item{stem_stride_rt, stem_stride_cv}{Must match encoder configuration.}

\item{encoder_channels}{Length-5 integer vector, same as the encoder's.
Default \code{c(16L, 16L, 32L, 64L, 64L)} matches previous behavior.}

\item{dropout_p}{Encoder-side channel dropout probability
(see \code{\link[=dms_encoder]{dms_encoder()}}). Decoder stays clean — dropout is only
applied to the encoder because that's the part transferred to
downstream classification. Default \code{0.0}.}
}
\description{
Forward pass: \code{x -> encoder -> z -> decoder -> reconstruction}. Trained
on synthetic images (noisy input, clean target) via MSE loss in the
pretraining functions; the resulting encoder transfers to downstream
classification.
}

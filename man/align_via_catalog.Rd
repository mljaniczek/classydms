% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_peaks.R
\name{align_via_catalog}
\alias{align_via_catalog}
\title{Align samples via cluster-membership offsets}
\usage{
align_via_catalog(
  peak_params,
  eps_rt_loose = 0.03,
  eps_cv_loose = 0.08,
  min_support_frac = 0.1,
  min_samples_per_anchor = NULL,
  min_anchors_per_sample = 5L,
  iterate = TRUE,
  max_iter = 3L,
  verbose = TRUE
)
}
\arguments{
\item{peak_params}{\code{peak_params} object with \code{sample_idx_raw}.}

\item{eps_rt_loose, eps_cv_loose}{Preliminary-catalog clustering eps.
Must be \strong{larger than the expected drift magnitude} so drifted
samples' peaks join the same cluster as un-drifted samples' peaks.
Defaults \verb{(0.03, 0.08)} accommodate up to ~3\% RT drift.}

\item{min_support_frac}{Support fraction for the preliminary catalog.}

\item{min_samples_per_anchor}{A compound must have at least this
many unique samples to serve as an alignment anchor. \code{NULL}
(default) sets it to \code{max(3, floor(0.30 * n_samples))} so only
well-supported compounds contribute offsets.}

\item{min_anchors_per_sample}{A sample won't be shifted if fewer
anchor compounds contain a peak from it.}

\item{iterate}{Whether to rebuild catalog on aligned peaks and
re-align. Default \code{TRUE}.}

\item{max_iter}{Maximum iterations.}

\item{verbose}{Whether to print progress.}
}
\value{
Updated \code{peak_params} with aligned \code{rt_loc_raw},
\code{cv_loc_raw}, and an \code{alignment} field with method =
\code{"cluster_membership"}, \code{shifts_rt}, \code{shifts_cv},
\code{n_anchors_matched} (per sample), \code{n_anchors_total}, \code{iterations},
\code{converged}.
}
\description{
Alternative alignment approach that avoids the distance-matching
fragility of \code{\link[=align_peak_params]{align_peak_params()}}. Steps:
}
\details{
\enumerate{
\item Build a preliminary catalog with \strong{loose} eps (large enough
to capture drifted peaks in the same cluster as un-drifted ones)
\item For each anchor compound with sufficient sample support,
each member peak has a known offset from the cluster centroid
\item For each sample, aggregate its member-peak offsets across all
anchor compounds — the median gives that sample's drift estimate
\item Apply per-sample shifts to all peaks
\item Optionally iterate: rebuild catalog on aligned peaks and
refine
}

Why this is more robust than distance-matching: peak-to-anchor
correspondences are established by the preliminary clustering, not
by nearest-neighbor search. In dense peak clouds where many
unrelated peaks fall near each anchor, distance-matching gets
confused; cluster-membership does not.

Signals that this alignment is helping (after building the final
catalog on aligned peaks vs original): \code{n_compounds} decreases
(fragments consolidate), \code{n_singletons} decreases, \code{median_prevalence}
increases, and \code{n_anchor_90pct} increases.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_catalog.R
\name{split_catalog}
\alias{split_catalog}
\title{Split a catalog into two disjoint sub-catalogs by compound}
\usage{
split_catalog(catalog, frac = 0.5, seed = NULL)
}
\arguments{
\item{catalog}{A \code{peak_catalog} object.}

\item{frac}{Fraction of compounds to place in the first split.
Default \code{0.5}.}

\item{seed}{Optional RNG seed for reproducible partitioning.}
}
\value{
A list with \code{a} and \code{b}, each a valid \code{peak_catalog} with
its own compound subset. \code{a} also carries any \code{small_clusters}
and \code{singletons} from the original.
}
\description{
Randomly partitions the catalog's compounds into two sets. Useful
for encoder-transfer experiments: pretrain the encoder on
synthetic data generated from sub-catalog A, then simulate
case/control data from sub-catalog B — biomarker compounds and
background compounds in B are all guaranteed to have been unseen
during pretraining.
}
\details{
Small clusters and singletons are left untouched (attached to the
first split only) since they're not typically used for synthetic
generation.
}

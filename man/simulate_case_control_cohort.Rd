% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_case_control.R
\name{simulate_case_control_cohort}
\alias{simulate_case_control_cohort}
\title{Simulate a case / control cohort with known injected biomarkers}
\usage{
simulate_case_control_cohort(
  background_peak_params,
  biomarkers,
  n_cases,
  n_controls,
  H,
  W,
  size_jitter = 0.15,
  location_jitter_rt = 2,
  location_jitter_cv = 1,
  add_noise = TRUE,
  seed = NULL
)
}
\arguments{
\item{background_peak_params}{Modified \code{peak_params} returned by
\code{\link[=holdout_biomarkers]{holdout_biomarkers()}} (or the same, minus the biomarker peaks).}

\item{biomarkers}{Output of \code{\link[=build_biomarker_spec]{build_biomarker_spec()}} (or a compatible
tibble with the same columns).}

\item{n_cases, n_controls}{Number of samples in each group.}

\item{H, W}{Output image dimensions.}

\item{size_jitter, location_jitter_rt, location_jitter_cv}{Passed to
the background generator; also applied to biomarker peaks so their
morphology varies per sample.}

\item{add_noise}{Whether to add background noise (default TRUE).}

\item{seed}{Optional RNG seed.}
}
\value{
A list with:
\describe{
\item{samples}{named list of length \code{n_cases + n_controls}. Each
element is \code{list(path, time, cv, Z)} matching the shape produced
by \code{\link[=load_dms_directory]{load_dms_directory()}}, so it can flow through the rest of
the classydms pipeline unchanged.}
\item{y}{named integer vector: 2 = case, 1 = control.}
\item{ground_truth}{the biomarkers tibble.}
\item{per_sample_firing}{logical matrix (\code{n_samples} x
\code{n_biomarkers}) recording which biomarkers actually fired in
each sample, for post-hoc analysis.}
}
}
\description{
Generates a synthetic cohort of \code{n_cases} case samples and
\code{n_controls} control samples. Each sample gets:
\enumerate{
\item A background drawn from \code{background_peak_params} via
\code{\link[=generate_one_synthetic]{generate_one_synthetic()}} with default \code{location_mode = "empirical"} and \code{attribute_mode = "joint"} (add_noise = FALSE
at this stage; noise is added after biomarkers).
\item For each biomarker row: a Bernoulli(\code{case_prevalence} or
\code{control_prevalence}) draw decides whether the biomarker
"fires" in this sample. If it fires, a 2D Gaussian peak with
the biomarker's \verb{(rt_loc, cv_loc, sigma_rt, sigma_cv)} and
intensity \code{intensity * (case_intensity_mult | control_intensity_mult)}
is added, plus per-peak jitter matching production
(\code{location_jitter_rt/cv}, \code{size_jitter}).
\item Folded-Normal background noise (from
\code{background_peak_params$noise}).
}
}

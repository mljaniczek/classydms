% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_catalog.R
\name{build_universal_catalog}
\alias{build_universal_catalog}
\title{Build a universal catalog by pooling all samples from multiple cohorts}
\usage{
build_universal_catalog(
  pp_list,
  eps_rt = 0.01,
  eps_cv = 0.05,
  min_support_frac = 0.1,
  min_cluster_size = 2L,
  coord_mode = c("fraction", "physical"),
  align = FALSE
)
}
\arguments{
\item{pp_list}{Named list of \code{peak_params} objects.}

\item{eps_rt, eps_cv}{Catalog clustering eps (fraction of image).}

\item{min_support_frac}{Support threshold. Applies to the combined
sample set — a compound must span at least this fraction of the
total N samples.}

\item{min_cluster_size}{Minimum peaks per cluster.}

\item{align}{Optional cross-cohort drift correction. \code{FALSE} (default)
skips alignment. \code{TRUE} calls \code{\link[=align_across_cohorts]{align_across_cohorts()}} on
\code{pp_list} with sensible defaults (\code{eps_rt_loose = 3 * eps_rt},
\code{eps_cv_loose = 3 * eps_cv}, \code{min_support_frac = min_support_frac},
\code{iterate = TRUE}, \code{max_iter = 3L}), then builds the universal
catalog on the aligned cohorts. Corrects BOTH intra-cohort and
inter-cohort drift in one step. Pass a named list to override
any of those. Setting \code{align = TRUE} is a defensible answer to
reviewer concerns about systematic per-sample or per-batch
drift; the compactness-delta table in \code{compare_all_catalogs.Rmd}
shows whether it helped on your cohorts.}
}
\value{
A \code{peak_catalog} object with a \code{per_source_prevalence}
list-column on \code{compounds} giving cohort-level prevalence, and
\code{source_names} / \code{source_n_samples} fields in \code{parameters}.
}
\description{
Unlike \code{\link[=merge_catalogs]{merge_catalogs()}}, which reconciles already-built per-cohort
catalogs, this function pools raw peaks from all cohorts BEFORE
clustering. Every peak from every sample contributes directly to
catalog construction, so systematic per-cohort offsets get absorbed
into the compound centroids rather than producing duplicate entries.
}
\details{
Each catalog compound gets an extra list-column
\code{per_source_prevalence} recording the fraction of each input
cohort's samples in which the compound was observed. This mirrors
\code{\link[=merge_catalogs]{merge_catalogs()}}'s output for downstream compatibility.
}

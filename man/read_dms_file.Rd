% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{read_dms_file}
\alias{read_dms_file}
\title{Read one raw GC-DMS data file}
\usage{
read_dms_file(path)
}
\arguments{
\item{path}{File path to one GC-DMS file.}
}
\value{
List with \code{path}, \code{time} (numeric vector), \code{cv} (numeric vector),
and \code{Z} (numeric matrix of intensity, rows = RT, cols = CV).
}
\description{
Expects a tab-delimited file with this layout:
\itemize{
\item Line 1: literal "Vc"
\item Line 2: tab + compensation-voltage (CV) values, tab-separated
\item Line 3: header row (typically "Time Stamp" followed by per-CV labels)
\item Lines 4+: each row begins with a retention time (in seconds),
followed by intensity values for each CV
}
}
\details{
Implemented with base R only (no readr / stringr dependency) so it
works regardless of which packages the user has attached.
}

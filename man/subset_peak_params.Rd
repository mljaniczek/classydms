% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_catalog.R
\name{subset_peak_params}
\alias{subset_peak_params}
\title{Subset a peak_params object to a chosen set of samples}
\usage{
subset_peak_params(peak_params, sample_indices)
}
\arguments{
\item{peak_params}{A \code{peak_params} object with \code{sample_idx_raw}.}

\item{sample_indices}{Integer vector of sample indices (in the
original peak_params' 1..n_samples space) to keep. Common
pattern: \code{which(y == disease_label)}.}
}
\value{
A \code{peak_params}-shaped list containing only the specified
samples' peaks. Ready to pass to \code{\link[=build_peak_catalog]{build_peak_catalog()}} and other
downstream tools.
}
\description{
Given a \code{peak_params} object with \code{sample_idx_raw} (or after
\code{\link[=backfill_sample_idx_raw]{backfill_sample_idx_raw()}}), returns a new \code{peak_params} containing
only the peaks and metadata for the specified samples. Useful for
building per-condition catalogs from a single pooled
\code{estimate_peak_params} run — split a combined disease+control
\code{peak_params} into \code{pp_disease} and \code{pp_control} without rerunning
the expensive step.
}
\details{
Sample indices in the returned object are remapped to \code{1:length(sample_indices)}
so downstream tools that expect contiguous 1-based indexing work.
\code{sample_names}, if present, are subset to match. Fitted summary
statistics (\code{rt_loc$mean/sd}, \code{sigma_rt$meanlog/sdlog}, etc.) are
recomputed from the subset; noise stats are kept as-is (pooled
across samples in the original run, which is usually close enough
unless the condition of interest has systematically different
background noise).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpretability.R
\name{compute_saliency_map}
\alias{compute_saliency_map}
\title{Compute a Class Activation Map (CAM) for one sample}
\usage{
compute_saliency_map(encoder, Z, coefs, device = "cpu")
}
\arguments{
\item{encoder}{A pre-trained \code{dms_encoder}.}

\item{Z}{One sample's preprocessed, padded matrix.}

\item{coefs}{Numeric vector of length equal to the encoder's latent
dim (elastic net coefficients on the GAP features).}

\item{device}{"cpu" or "cuda".}
}
\value{
List with \code{raw_cam} and \code{upsampled_cam}.
}
\description{
Weights each encoder channel's spatial activation by its elastic net
coefficient (\code{coefs}), summing across channels to produce a 2-D
saliency map. Because the elastic net classifier head is linear in
the global-average-pooled features, this construction is mathematically
equivalent to Class Activation Mapping using elastic net coefficients
as head weights (and similar in spirit to Grad-CAM, without requiring
backpropagation).
}
\details{
Returns the saliency at the encoder output resolution (raw_cam) and
bilinearly upsampled to the input dimensions (upsampled_cam).
}

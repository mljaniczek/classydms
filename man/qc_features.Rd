% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{qc_features}
\alias{qc_features}
\title{Compute per-sample QC features}
\usage{
qc_features(samples, sample_names = NULL, compute_cohort_correlation = FALSE)
}
\arguments{
\item{samples}{A list of sample objects, each with element \code{Z} (the
preprocessed intensity matrix). Typically the output of
\code{lapply(samples, trim_sample, bounds = global_b)}.}

\item{sample_names}{Optional character vector of sample names. If
\code{NULL}, uses \code{names(samples)} or falls back to integer indices.}

\item{compute_cohort_correlation}{If \code{TRUE}, also compute each
sample's correlation against the cohort median spectrum. Slower
(O(n_samples * n_pixels)) but useful for detecting morphological
outliers that don't trigger univariate flags. Default \code{FALSE}.}
}
\value{
A tibble with one row per sample and columns:
\itemize{
\item \code{sample} — sample identifier
\item \code{rt_dim}, \code{cv_dim} — post-trim dimensions
\item \code{n_positive}, \code{prop_nonzero} — count and fraction of pixels above zero
\item \code{total_signal} — sum of all intensities
\item \code{mean_positive} — mean of pixels above zero
\item \code{max_intensity} — peak pixel value
\item \code{dynamic_range} — \verb{max_intensity / median(positive pixels)}
\item \code{bbox_size} — \code{rt_dim * cv_dim}
\item \code{n_peaks} — detected peaks via \code{\link[=pick_peak_centers]{pick_peak_centers()}}
\item \code{cohort_correlation} — (if requested) correlation with cohort median
}
}
\description{
Calculates a battery of sample-level summary statistics on a list of
preprocessed sample matrices (typically after Savitzky-Golay + AsLS +
dust thresholding + trim, but before normalization or padding).
Returns a tibble with one row per sample and columns covering signal
intensity, sparsity, peak density, and (optional) cohort-relative
similarity.
}
\details{
The returned table is consumed by \code{\link[=flag_junk_samples]{flag_junk_samples()}}, which
applies user-tunable thresholds to mark samples for exclusion.
}

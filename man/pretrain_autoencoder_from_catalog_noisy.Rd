% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretrain.R
\name{pretrain_autoencoder_from_catalog_noisy}
\alias{pretrain_autoencoder_from_catalog_noisy}
\title{Pre-train the encoder from a peak catalog using three-layer noisy samples (smoke test only)}
\usage{
pretrain_autoencoder_from_catalog_noisy(
  catalog,
  H,
  W,
  anchor_ids,
  variable_config = NULL,
  contamination_config = NULL,
  n_synthetic = 2000L,
  epochs = 30L,
  batch_size = 32L,
  lr = 0.001,
  weight_decay = 1e-04,
  add_noise = TRUE,
  stem_stride_rt = 4L,
  stem_stride_cv = 1L,
  device = if (torch::cuda_is_available()) "cuda" else "cpu",
  seed = 42L
)
}
\arguments{
\item{catalog}{A \code{peak_catalog} object.}

\item{H, W}{Synthetic image dimensions.}

\item{anchor_ids}{Integer vector of compound_ids to force-fire in
every sample (the invariant backbone). Typically the ~100 anchors
at prevalence >= 0.9 from the universal catalog.}

\item{variable_config, contamination_config}{Passed through to
\code{\link[=generate_noisy_pretrain_sample]{generate_noisy_pretrain_sample()}}. Pass \code{NULL} for either to
skip that layer. See that function's docs for field details.}

\item{n_synthetic}{Number of synthetic samples generated.}

\item{epochs, batch_size, lr, weight_decay}{Optimization parameters.}

\item{add_noise}{If TRUE, denoising autoencoder; if FALSE,
reconstruction.}

\item{stem_stride_rt, stem_stride_cv}{Encoder stem strides.}

\item{device}{"cpu" or "cuda".}

\item{seed}{RNG seed.}
}
\value{
List with \code{encoder}, \code{autoencoder}, \code{loss_history}, and the
\code{variable_config} / \code{contamination_config} actually used
(echoed for provenance).
}
\description{
Same training loop as \code{\link[=pretrain_autoencoder_from_catalog]{pretrain_autoencoder_from_catalog()}} but each
synthetic sample is generated via
\code{\link[=generate_noisy_pretrain_sample]{generate_noisy_pretrain_sample()}} — an anchor backbone plus
optional variable-catalog and contamination layers. The extra
variability is intended to teach the encoder to expect real-world
skin-VOC characteristics: consistent backbone, per-sample dietary /
host-genetic peaks, and contamination-like unfamiliar peaks.
}
\details{
\strong{For real pretraining runs prefer
\code{\link[=pretrain_denoising_online]{pretrain_denoising_online()}} with \code{catalog = your_catalog},
\code{anchor_ids = ids}, and the same \code{variable_config} /
\code{contamination_config}} — that path is online (fresh batches per
step) so training data isn't capped at \code{n_synthetic}. This pre-alloc
version stays available for smoke tests.

To reproduce the current \code{pretrain_autoencoder_from_catalog()}
behavior, pass \code{variable_config = NULL} and \code{contamination_config = NULL}
— the samples will contain only the anchor set (fired at 100 percent).
}

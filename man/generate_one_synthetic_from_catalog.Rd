% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_generation.R
\name{generate_one_synthetic_from_catalog}
\alias{generate_one_synthetic_from_catalog}
\title{Generate one synthetic sample from a peak catalog}
\usage{
generate_one_synthetic_from_catalog(
  catalog,
  H,
  W,
  add_noise = TRUE,
  size_jitter = 0.15,
  location_jitter_rt = 2,
  location_jitter_cv = 1,
  noise_scale = 1,
  prevalence_override = NULL,
  intensity_mult = NULL,
  compound_ids = NULL,
  peak_shape = c("gaussian", "emg", "mix"),
  emg_tau_range = c(0.2, 0.8),
  emg_mix_prob = 0.3
)
}
\arguments{
\item{catalog}{A \code{peak_catalog} object from \code{\link[=build_peak_catalog]{build_peak_catalog()}}.}

\item{H, W}{Output image dimensions in pixels.}

\item{add_noise}{Whether to add folded-Normal background noise.}

\item{size_jitter}{Per-peak log-Normal size scale factor SD.}

\item{location_jitter_rt, location_jitter_cv}{Per-peak location
jitter SD (pixels).}

\item{noise_scale}{Multiplier on \code{catalog$noise$sd}. Default \code{1.0}.}

\item{prevalence_override}{Optional named numeric vector mapping
\code{compound_id} (as character) to a replacement prevalence. Not
present compounds use the catalog's own prevalence.}

\item{intensity_mult}{Optional named numeric vector mapping
\code{compound_id} (as character) to an amplitude multiplier applied
when the compound fires. Defaults to \code{1.0} for unlisted compounds.}

\item{compound_ids}{Optional integer vector of compound_ids to
include; \code{NULL} (default) uses every catalog compound.}
}
\value{
List with \code{clean} (H x W), \code{noisy} (H x W, \verb{= clean} when
\code{add_noise = FALSE}), and \code{fired} (logical vector of length
\code{nrow(catalog$compounds)} indicating which compounds actually
appeared in this sample).
}
\description{
For each compound in the catalog, a \code{Bernoulli(prevalence)} draw
decides whether the compound "fires" in this sample. If it fires, a
2-D Gaussian peak is placed at \verb{compound$rt_loc, compound$cv_loc}
with morphology bootstrapped from the compound's observed
\verb{(sigma_rt, sigma_cv, intensity)} triples. Per-peak location and
size jitter apply on top. Folded-Normal noise is added at the end
when \code{add_noise = TRUE}.
}
\details{
Compared to \code{\link[=generate_one_synthetic]{generate_one_synthetic()}}, the catalog version reflects
the real-data property that the same compound positions appear
across many samples. Which specific compounds appear per sample is
random (via Bernoulli), but the compound \emph{identities} and positions
are shared across all synthetic samples.
}

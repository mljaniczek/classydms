% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{flag_junk_samples}
\alias{flag_junk_samples}
\title{Flag samples that fail QC checks}
\usage{
flag_junk_samples(
  qc_tbl,
  min_prop_nonzero = 0.1,
  max_saturation = Inf,
  min_dynamic_range = 5,
  iqr_mult = 3,
  min_cohort_correlation = 0.3,
  features_for_iqr = c("total_signal", "mean_positive", "n_peaks", "prop_nonzero",
    "max_intensity", "dynamic_range", "bbox_size")
)
}
\arguments{
\item{qc_tbl}{Output of \code{\link[=qc_features]{qc_features()}}.}

\item{min_prop_nonzero}{Threshold below which a sample is flagged as
low-signal (default 0.10).}

\item{max_saturation}{Threshold at or above which a sample is flagged
for saturation (default \code{Inf} = disabled; set to e.g. 65535 for
16-bit detectors, or whatever the instrument's max readout is).}

\item{min_dynamic_range}{Threshold below which dynamic range is
considered too low (default 5).}

\item{iqr_mult}{Multiplier for IQR-based outlier detection across
univariate features (default 3).}

\item{min_cohort_correlation}{Threshold below which a sample's
correlation with the cohort median spectrum triggers a flag
(default 0.3; only applied if \code{cohort_correlation} is in the table).}

\item{features_for_iqr}{Character vector of QC feature names to
include in IQR outlier detection. Default uses the package-recommended
set; pass NULL to disable IQR checks entirely.}
}
\value{
The input tibble with additional columns: one \verb{*_outlier}
column per IQR-checked feature, plus \code{low_signal}, \code{saturation},
\code{low_dynamic_range}, \code{low_cohort_correlation}, and a final
\code{flagged} column that is the OR of all criteria.
}
\description{
Applies threshold-based and IQR-based criteria to a QC table from
\code{\link[=qc_features]{qc_features()}} and returns the table with boolean flag columns
added. A sample is marked \code{flagged = TRUE} if it triggers any of
the active criteria. Thresholds are tunable; pass \code{NA} or \code{Inf} to
disable a specific check.
}
\details{
Default criteria (Tier 1 + Tier 2 from the package design):
\itemize{
\item \strong{Low signal}: \code{prop_nonzero} below \code{min_prop_nonzero}.
\item \strong{Saturation}: \code{max_intensity} at or above \code{max_saturation}.
\item \strong{Low dynamic range}: \code{dynamic_range} below \code{min_dynamic_range}.
\item \strong{Univariate IQR outliers} on \code{total_signal}, \code{mean_positive},
\code{n_peaks}, \code{prop_nonzero}, \code{max_intensity}, \code{dynamic_range}, and
\code{bbox_size} (any feature more than \code{iqr_mult} IQR from the cohort
median triggers).
\item \strong{Low cohort correlation} (only if \code{cohort_correlation} is in
the table): correlation below \code{min_cohort_correlation}.
}
}

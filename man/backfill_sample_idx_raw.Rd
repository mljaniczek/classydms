% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_catalog.R
\name{backfill_sample_idx_raw}
\alias{backfill_sample_idx_raw}
\title{Backfill sample_idx_raw and sample_names on an older peak_params}
\usage{
backfill_sample_idx_raw(peak_params, sample_names = NULL)
}
\arguments{
\item{peak_params}{An older \code{peak_params} object missing
\code{sample_idx_raw}.}

\item{sample_names}{Optional character vector of sample names.
Must have length \code{n_samples}. If \code{NULL}, \code{sample_names} is not
attached (the catalog doesn't need it; only the peak-params
report does).}
}
\value{
A \code{peak_params}-shaped list with \code{sample_idx_raw} and
(if provided) \code{sample_names} added.
}
\description{
\code{sample_idx_raw} and \code{sample_names} were added to
\code{\link[=estimate_peak_params]{estimate_peak_params()}} in a recent commit. \code{peak_params} objects
saved before then lack these fields and cannot be used with
\code{\link[=build_peak_catalog]{build_peak_catalog()}} directly. This helper reconstructs
\code{sample_idx_raw} from the per-sample peak count already stored in
\code{n_peaks$values} and optionally attaches \code{sample_names} from a
user-supplied vector.
}
\details{
Peaks in the \verb{_raw} vectors are stored in sample-then-detection
order (all of sample 1's peaks, then all of sample 2's peaks, ...),
and each sample contributes exactly \code{n_peaks$values[i]} entries.
The reconstruction is \code{rep(1:n_samples, n_peaks$values)}.

Sanity-checks that \code{sum(n_peaks$values) == length(rt_loc_raw)}; if
this fails, the peak_params object is inconsistent and must be
regenerated by rerunning \code{estimate_peak_params}.
}

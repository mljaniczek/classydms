% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data.R
\name{generate_synthetic_dataset}
\alias{generate_synthetic_dataset}
\title{Generate N synthetic pairs as torch tensors}
\usage{
generate_synthetic_dataset(
  params,
  N,
  H,
  W,
  add_noise = TRUE,
  size_jitter = 0.6,
  normalize = TRUE,
  dust_threshold = 0,
  location_mode = c("empirical", "marginal"),
  location_jitter_rt = 2,
  location_jitter_cv = 1,
  attribute_mode = c("joint", "marginal")
)
}
\arguments{
\item{params}{Output of \code{\link[=estimate_peak_params]{estimate_peak_params()}}.}

\item{N}{Number of synthetic samples to generate.}

\item{H, W}{Output image dimensions.}

\item{add_noise}{If TRUE, add spatially-varying background noise.}

\item{size_jitter}{Per-peak random log-normal scale factor SD for size
diversity (default 0.6).}

\item{normalize}{Whether to apply \code{\link[=normalize_sample]{normalize_sample()}} (default TRUE).}

\item{dust_threshold}{If > 0, zero out pixels below this value in both
the clean and noisy synthetic images BEFORE normalization. This
matches the dust-thresholding step applied to real data via
\code{\link[=baseline_basement]{baseline_basement()}} and produces synthetic samples whose sparsity
profile matches real samples. Default 0 (no dust thresholding,
backward-compatible). To match real-data preprocessing, set this
to the same \code{basement_thr} you passed to \code{\link[=baseline_basement]{baseline_basement()}}.}

\item{location_mode}{How to sample peak (RT, CV) locations. \code{"empirical"}
(default, recommended) draws each peak's location by picking a random
observed peak location from the real cohort (from \code{params$rt_loc_raw}
/ \code{params$cv_loc_raw}) and adding small per-peak jitter. This produces
synthetic images whose peaks cluster at cohort-typical hotspots
rather than scattering uniformly. \code{"marginal"} uses independent
Normal draws on each axis with mean/SD from \code{params$rt_loc$mean}/\code{sd}
— the previous behavior, kept for backward compatibility.}

\item{location_jitter_rt, location_jitter_cv}{SD of per-peak location
jitter (in pixels) added on top of the empirical draw. Represents
instrument reproducibility. Default 2 pixels RT, 1 pixel CV.}

\item{attribute_mode}{How to sample per-peak morphology
(sigma_rt, sigma_cv, intensity). \code{"joint"} (default, recommended)
draws an index \code{j} into \code{params$rt_loc_raw} and reuses the observed
\verb{(sigma_rt_raw[j], sigma_cv_raw[j], intensity_raw[j])} triple.
This preserves the empirical joint distribution: wider peaks are
drawn with correspondingly higher intensity, matching the physical
coupling in real GC-DMS data. When \code{location_mode == "empirical"}
the same \code{j} is used for location and morphology; otherwise an
independent index is drawn. \code{"marginal"} reverts to independent
log-normal draws for each attribute (the pre-\code{attribute_mode}
behavior; wider peaks and higher intensity are uncorrelated).}
}
\value{
List with \code{clean} and \code{noisy} 4-D torch tensors.
}
\description{
Calls \code{\link[=generate_one_synthetic]{generate_one_synthetic()}} N times, applies per-sample
log-quantile normalization, and stacks the result as torch tensors
of shape \verb{(N, 1, H, W)}.
}

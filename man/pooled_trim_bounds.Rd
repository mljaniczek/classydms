% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{pooled_trim_bounds}
\alias{pooled_trim_bounds}
\title{Pool per-sample trim bounds to a single cohort-wide bounding box}
\usage{
pooled_trim_bounds(bounds_list, R, C, q_lo = 0.05, q_hi = 0.95)
}
\arguments{
\item{bounds_list}{List of bounds (one per sample) from
\code{\link[=trim_bounds_from_occupancy]{trim_bounds_from_occupancy()}}.}

\item{R, C}{Maximum row / column counts across samples; the pooled
bounds are clamped to lie within \verb{[1, R]} and \verb{[1, C]}.}

\item{q_lo, q_hi}{Quantiles for pooling start / end positions
(default 0.05 and 0.95).}
}
\value{
List with \code{rt_start}, \code{rt_end}, \code{cv_start}, \code{cv_end} giving
the cohort-wide bounding box.
}
\description{
Uses robust quantiles (5th of starts, 95th of ends by default) so a
single outlier sample with unusually narrow or wide content does not
force the cohort-wide bounds to lose data common to nearly all
samples.
}

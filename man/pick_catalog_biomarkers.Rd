% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_generation.R
\name{pick_catalog_biomarkers}
\alias{pick_catalog_biomarkers}
\title{Pick a set of catalog compounds as biomarkers}
\usage{
pick_catalog_biomarkers(
  catalog,
  n_biomarkers,
  min_prevalence = 0,
  max_prevalence = 1,
  min_intensity_quantile = NULL,
  seed = NULL
)
}
\arguments{
\item{catalog}{A \code{peak_catalog} object.}

\item{n_biomarkers}{Number of biomarker compound_ids to pick.}

\item{min_prevalence, max_prevalence}{Only consider compounds whose
catalog prevalence falls in this range. Defaults \verb{[0, 1]}
(any). For "rare-in-cohort" biomarkers set e.g. \code{max = 0.3}; for
"common-in-cohort" set \code{min = 0.7}.}

\item{min_intensity_quantile}{Optional threshold on the median of
each compound's \code{intensity_obs}. Same idea as in
\code{\link[=holdout_biomarkers]{holdout_biomarkers()}}.}

\item{seed}{Optional RNG seed.}
}
\value{
Integer vector of length \code{n_biomarkers} — the selected
compound_ids.
}
\description{
Selects \code{n_biomarkers} compound_ids uniformly at random from the
catalog, optionally filtered by prevalence range or intensity
quantile. Analogous to \code{\link[=holdout_biomarkers]{holdout_biomarkers()}} for the peak_params
flow.
}
\details{
Unlike the peak_params version, no "protect radius" is needed —
catalog compounds already sit at discrete positions with no
overlap between them, so background compounds (all the non-selected
entries) can't land in biomarker regions.
}

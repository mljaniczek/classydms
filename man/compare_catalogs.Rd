% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_compare.R
\name{compare_catalogs}
\alias{compare_catalogs}
\title{Compare two peak catalogs by compound proximity}
\usage{
compare_catalogs(
  catalog_a,
  catalog_b,
  name_a = "a",
  name_b = "b",
  match_radius_rt = 0.01,
  match_radius_cv = 0.05
)
}
\arguments{
\item{catalog_a, catalog_b}{\code{peak_catalog} objects.}

\item{name_a, name_b}{Short labels used as column-name suffixes in
the \code{shared} output. Default \code{"a"} and \code{"b"}.}

\item{match_radius_rt, match_radius_cv}{Maximum distance to count a
compound as matched. Defaults \verb{(0.01, 0.05)} — the same defaults
as \code{build_peak_catalog}.}
}
\value{
List with:
\describe{
\item{shared}{tibble of matched compound pairs with
\verb{compound_id_<a>}, \verb{compound_id_<b>}, \verb{rt_loc_<a>},
\verb{rt_loc_<b>}, \verb{cv_loc_<a>}, \verb{cv_loc_<b>},
\verb{prevalence_<a>}, \verb{prevalence_<b>}, \code{prevalence_delta}
(b − a), and \code{spatial_distance} (Euclidean in the fraction
space).}
\item{only_a}{tibble of compounds present in A but not matched
in B.}
\item{only_b}{tibble of compounds present in B but not matched
in A.}
\item{summary}{one-row tibble of counts + fractions.}
}
}
\description{
Matches each compound in \code{catalog_a} to its nearest neighbor in
\code{catalog_b} by spatial proximity in fraction-of-image \verb{(rt_loc, cv_loc)} space. Compounds are "matched" if the nearest neighbor is
within \verb{(match_radius_rt, match_radius_cv)}. Returns three data
frames: shared compounds (matched to each other), compounds only in
A, and compounds only in B. Also reports the per-catalog prevalence
for shared compounds so you can see whether the same compound
expresses at different rates between cohorts.
}

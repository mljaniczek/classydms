% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_generation.R
\name{generate_noisy_pretrain_sample}
\alias{generate_noisy_pretrain_sample}
\title{Generate one three-layer synthetic sample for encoder pretraining}
\usage{
generate_noisy_pretrain_sample(
  catalog,
  H,
  W,
  anchor_ids,
  variable_config = NULL,
  contamination_config = NULL,
  add_noise = TRUE,
  size_jitter = 0.15,
  location_jitter_rt = 2,
  location_jitter_cv = 1,
  noise_scale = 1,
  anchor_dropout = 0,
  anchor_jitter_variability = NULL,
  variable_source_peak_params = NULL
)
}
\arguments{
\item{catalog}{A \code{peak_catalog} object.}

\item{H, W}{Output image dimensions in pixels.}

\item{anchor_ids}{Integer vector of compound_ids to force-fire.
Typically the ~100 anchors from the universal catalog with
prevalence >= 0.9.}

\item{variable_config}{Optional list controlling the variable layer.
Fields:
\itemize{
\item \code{n_per_sample}: scalar (fixed count) or length-2 numeric (uniform
range) for how many variable compounds to draw. Default
\code{c(30, 80)}.
\item \code{prevalence_range}: length-2 numeric giving the catalog
prevalence range eligible for the variable pool. Default
\code{c(0.05, 0.5)}.
\item \code{intensity_scale}: length-2 numeric giving amplitude multiplier
bounds. Default \code{c(0.3, 1.0)} (variable compounds slightly
dimmer than anchors on average).
\item \code{exclude_anchors}: \code{TRUE} (default) — remove anchor_ids from the
variable pool so anchors don't get drawn twice.
Pass \code{NULL} to skip this layer.
}}

\item{contamination_config}{Optional list controlling contamination
injection. Same fields as \code{off_catalog_peaks} in
\code{\link[=simulate_case_control_from_catalog]{simulate_case_control_from_catalog()}} except with \code{n_per_sample}
instead of \code{n_cases} / \code{n_controls}. Default field values:
\code{n_per_sample = c(5, 30)}, \code{rt_range = c(0.05, 0.95)},
\code{cv_range = c(0.05, 0.95)}, \code{intensity_range = c(0.1, 0.5)},
\code{exclusion_eps_rt = 0.02}, \code{exclusion_eps_cv = 0.05}. Pass \code{NULL}
to skip this layer.}

\item{add_noise}{Whether to add folded-Normal background noise on
top of the three layers.}

\item{size_jitter, location_jitter_rt, location_jitter_cv}{Per-peak
jitter parameters (same convention as
\code{\link[=generate_one_synthetic_from_catalog]{generate_one_synthetic_from_catalog()}}).}

\item{noise_scale}{Multiplier on catalog noise SD.}

\item{anchor_dropout}{Per-sample Bernoulli probability of dropping
each anchor before rendering. \code{0} (default) fires all anchors.}

\item{anchor_jitter_variability}{Optional length-2 range for a
per-sample scalar multiplier on \code{location_jitter_rt/cv}.
\code{NULL} (default) uses the fixed jitter values as-is.}

\item{variable_source_peak_params}{Optional \code{peak_params} bundle.
When provided, the variable layer is rendered via
\code{\link[=generate_one_synthetic]{generate_one_synthetic()}} on this bundle (real peaks, empirical
locations, joint attributes) instead of the catalog-clustered
render. Mutually exclusive with \code{variable_config}.}
}
\value{
List with \code{clean} (H x W), \code{noisy} (H x W), and per-layer
composition metadata (\code{anchor_ids}, \code{variable_ids},
\code{contamination_positions}).
}
\description{
Builds a synthetic GC-DMS image by stacking three layers of peaks:
}
\details{
\enumerate{
\item \strong{Anchor backbone.} Every compound in \code{anchor_ids} fires
unconditionally, with its own catalog morphology and normal
per-peak location / size jitter. These are the stable ~100
highly-prevalent compounds you want the encoder to treat as the
baseline landscape.
\item \strong{Variable catalog compounds} (optional). Per-sample random
subset of the catalog's less-prevalent compounds — filtered by
\code{variable_config$prevalence_range} and (optionally) excluding the
anchor pool. Simulates diet-related, host-genetics-related, and
other per-person volatiles. Intensity scaled by a per-peak factor
drawn from \code{variable_config$intensity_scale}.
\item \strong{Contamination} (optional). Uniform-random peaks placed at
positions NOT overlapping catalog compounds. Simulates sampling
apparatus contamination, environmental exposure, and truly
unfamiliar peaks. Sigmas drawn from the pooled catalog sigma
distribution so shapes look realistic.
}
}

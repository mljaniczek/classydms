% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretrained.R
\name{load_pretrained_encoder}
\alias{load_pretrained_encoder}
\title{Load a pre-trained encoder by name}
\usage{
load_pretrained_encoder(
  name,
  repo = "mljaniczek/classydms",
  tag = "pretrained-v0",
  with_autoencoder = FALSE,
  force_download = FALSE
)
}
\arguments{
\item{name}{Base name of the encoder file (without \code{.pt} extension).}

\item{repo}{GitHub repo.}

\item{tag}{Release tag.}

\item{with_autoencoder}{If TRUE, also fetch the autoencoder file and
return both as a list.}

\item{force_download}{Force re-download even if cached locally.}
}
\value{
Either a torch encoder module (if \code{with_autoencoder = FALSE})
or a list with \code{encoder}, \code{autoencoder}, and \code{manifest} elements.
}
\description{
Downloads the requested encoder file from GitHub Releases (cached
locally) and returns the loaded torch module. Pass \code{with_autoencoder = TRUE} to also fetch the full autoencoder (encoder + decoder), which
is needed for reconstruction QC but not for classification or saliency
mapping.
}
\examples{
\dontrun{
enc <- load_pretrained_encoder("flu_v1")
bundle <- load_pretrained_encoder("flu_v1", with_autoencoder = TRUE)
bundle$encoder; bundle$autoencoder; bundle$manifest
}
}

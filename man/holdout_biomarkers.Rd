% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_case_control.R
\name{holdout_biomarkers}
\alias{holdout_biomarkers}
\title{Hold out K observed peaks as biomarker templates}
\usage{
holdout_biomarkers(
  peak_params,
  n_biomarkers,
  protect_radius_rt = 15/1400,
  protect_radius_cv = 3/40,
  min_intensity_quantile = NULL,
  seed = NULL
)
}
\arguments{
\item{peak_params}{Output of \code{\link[=estimate_peak_params]{estimate_peak_params()}}; must contain
\code{rt_loc_raw}, \code{cv_loc_raw}, \code{sigma_rt_raw}, \code{sigma_cv_raw},
\code{intensity_raw}.}

\item{n_biomarkers}{Integer; number of biomarker templates to draw.}

\item{protect_radius_rt, protect_radius_cv}{Radii (in fractions of
image dimensions) around each biomarker inside which observed
peaks are also removed from the background pool. Default matches
the default \code{min_sep_rt}, \code{min_sep_cv} on a 1400 x 40 image (15 /
1400 and 3 / 40).}

\item{min_intensity_quantile}{Optional numeric in \verb{[0, 1)}. When
non-\code{NULL}, biomarker templates are drawn only from pool peaks
whose \code{intensity_raw} is at or above the given quantile of the
pool's intensity distribution — e.g. \code{0.90} restricts selection
to the top-10\% brightest peaks. Useful for sanity-check
scenarios where you want the injected biomarker to be visually
obvious even in a very sparse image. \code{NULL} (default) samples
uniformly from the whole pool.}

\item{seed}{Optional RNG seed for reproducible template selection.}
}
\value{
A list with:
\describe{
\item{biomarker_templates}{tibble with columns \code{rt_loc}, \code{cv_loc},
\code{sigma_rt}, \code{sigma_cv}, \code{intensity} — one row per held-out peak.}
\item{background_peak_params}{modified \code{peak_params} with the
held-out peaks and any peaks within the protected radius stripped
from every \verb{*_raw} vector. \code{n_peaks_detected} is updated.}
\item{n_pool_before}{pool size before hold-out.}
\item{n_pool_after}{pool size after hold-out.}
}
}
\description{
Uniformly at random draws \code{n_biomarkers} peak indices from
\code{peak_params}'s raw pools, extracts their locations and morphology
as biomarker templates, and returns a modified \code{peak_params} with
those peaks \emph{and} any peaks within a protected radius removed from
the raw pools. This guarantees that background samples drawn from
the returned \code{background_peak_params} will not include peaks that
land near biomarker locations, so injected biomarkers are truly
additive signal rather than contaminated by nearby background peaks.
}

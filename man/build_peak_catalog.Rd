% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_catalog.R
\name{build_peak_catalog}
\alias{build_peak_catalog}
\title{Build a peak catalog from a peak_params object}
\usage{
build_peak_catalog(
  peak_params,
  eps_rt = 0.01,
  eps_cv = 0.05,
  min_support_frac = 0.1,
  min_cluster_size = 2L,
  coord_mode = c("fraction", "physical")
)
}
\arguments{
\item{peak_params}{Output of \code{\link[=estimate_peak_params]{estimate_peak_params()}} containing
\code{rt_loc_raw}, \code{cv_loc_raw}, \code{sigma_rt_raw}, \code{sigma_cv_raw},
\code{intensity_raw}, \code{sample_idx_raw}, and \code{n_samples}.}

\item{eps_rt, eps_cv}{Clustering radius in fraction-of-image units.
Defaults \verb{(0.01, 0.05)} roughly match the peak-detector's
\verb{min_sep_rt = 8, min_sep_cv = 2} on a 1400 x 40 native image.
Tighter → more, smaller clusters. Looser → fewer, larger clusters.}

\item{min_support_frac}{Fraction of samples a cluster must span to
count as a compound. Default \code{0.10}: appear in at least 10\% of
samples.}

\item{min_cluster_size}{Minimum number of pool peaks in a cluster
for compound status. Default \code{2L} (excludes singletons from
compounds regardless of support).}
}
\value{
An object of class \code{peak_catalog} with fields:
\describe{
\item{compounds}{tibble of compound entries — one row per
compound with columns \verb{compound_id, rt_loc, cv_loc, location_sd_rt, location_sd_cv, n_observations, n_samples, prevalence}, and list-columns \verb{sigma_rt_obs, sigma_cv_obs, intensity_obs, member_indices}.}
\item{small_clusters}{same schema, for clusters below thresholds.}
\item{singletons}{same schema, for clusters of size 1.}
\item{parameters}{the settings used to build the catalog.}
}
}
\description{
Greedy clustering: peaks are sorted by intensity descending, each
unassigned peak becomes a new cluster seed, and all remaining
unassigned peaks within \verb{(eps_rt, eps_cv)} of the seed are added
to that cluster. Clusters are then classified into three groups:
\describe{
\item{compounds}{clusters with \verb{>= min_cluster_size} observations
AND spanning \verb{>= min_support_frac} of samples. These are the
"reliable positions" the catalog is built around.}
\item{small_clusters}{clusters with >= 2 observations but below
the sample-support threshold. Might be sporadic contamination,
small compounds, or peaks from just a few subjects.}
\item{singletons}{clusters of size 1 — peaks that didn't co-locate
with any other pool peak. Often noise, but worth inspecting.}
}
}
\details{
Uses a sorted-index optimization so runtime stays \verb{O(n log n + n·k)}
rather than \code{O(n^2)}, where \code{k} is the average number of candidates
per seed. Practical for pool sizes up to millions.
}

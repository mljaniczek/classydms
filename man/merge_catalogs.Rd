% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_compare.R
\name{merge_catalogs}
\alias{merge_catalogs}
\title{Merge multiple peak catalogs into a single unified catalog}
\usage{
merge_catalogs(
  catalog_list,
  names = NULL,
  match_radius_rt = NULL,
  match_radius_cv = NULL
)
}
\arguments{
\item{catalog_list}{List of \code{peak_catalog} objects.}

\item{names}{Optional character vector of length \code{length(catalog_list)}
naming each source. Default \code{"catalog_1"}, \code{"catalog_2"}, etc.}

\item{match_radius_rt, match_radius_cv}{Cross-catalog matching radii.
Defaults to the minimum \code{eps_rt} / \code{eps_cv} across the source
catalogs — using a larger radius than the source eps would cause
within-source compounds to fold into each other during the merge.}
}
\value{
A \code{peak_catalog} object with the merged compounds.
}
\description{
Combines compounds across catalogs by spatial proximity. Overlapping
compounds (within \verb{(match_radius_rt, match_radius_cv)} of each
other across catalogs) are merged into a single entry: observations
are concatenated, and prevalence is recomputed as a weighted average
by cohort size. Compounds unique to a single catalog are added as
new entries.
}
\details{
The merged catalog has an extra \code{per_source_prevalence} list-column
recording per-input-catalog prevalence for each merged compound —
useful for tracking which sources contributed which compounds.
}

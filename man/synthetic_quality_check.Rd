% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data.R
\name{synthetic_quality_check}
\alias{synthetic_quality_check}
\title{Quantitative comparison of synthetic vs real GC-DMS samples}
\usage{
synthetic_quality_check(
  Z_real_list,
  peak_params,
  H = NULL,
  W = NULL,
  n_synthetic = 50L,
  size_jitter = 0.6,
  normalize = FALSE,
  top_k = 10000L,
  dust_threshold = 0,
  location_mode = c("empirical", "marginal"),
  location_jitter_rt = 2,
  location_jitter_cv = 1,
  attribute_mode = c("joint", "marginal"),
  seed = NULL
)
}
\arguments{
\item{Z_real_list}{List of real preprocessed Z matrices. Typically
the same \code{Z_raw} you would pass to \code{\link[=estimate_peak_params]{estimate_peak_params()}}.}

\item{peak_params}{Output of \code{\link[=estimate_peak_params]{estimate_peak_params()}}.}

\item{H, W}{Dimensions for synthetic generation. Default uses the
dimensions of the first real sample.}

\item{n_synthetic}{Number of synthetic samples to generate.}

\item{size_jitter}{Same as in \code{\link[=generate_one_synthetic]{generate_one_synthetic()}}.}

\item{normalize}{If TRUE, apply \code{\link[=normalize_sample]{normalize_sample()}} to both real
and synthetic matrices before comparing. Default FALSE compares
on the raw (post-baseline-correction, pre-normalization) scale.}

\item{top_k}{Maximum peaks counted per sample for the n_peaks
comparison via \code{\link[=pick_peak_centers]{pick_peak_centers()}}. Set high enough to NOT cap
detected peak counts (e.g., 10000) so the comparison reflects
actual peak density rather than the cap. Default 10000.}

\item{dust_threshold}{If > 0, apply the same dust threshold to
synthetic samples that was applied to real data. Pass the same
value used in \code{\link[=baseline_basement]{baseline_basement()}} so synthetic and real are
compared on the same preprocessed sparsity profile.}

\item{location_mode}{Passed through to \code{\link[=generate_one_synthetic]{generate_one_synthetic()}}.
Default \code{"empirical"} samples peak locations from the real
cohort's observed hotspots.}

\item{location_jitter_rt, location_jitter_cv}{Passed through to
\code{\link[=generate_one_synthetic]{generate_one_synthetic()}}.}

\item{attribute_mode}{Passed through to \code{\link[=generate_one_synthetic]{generate_one_synthetic()}}.
Default \code{"joint"} couples per-peak sigma_rt / sigma_cv / intensity
by resampling the observed triple.}

\item{seed}{Optional RNG seed for reproducibility.}
}
\value{
A list with:
\item{summary}{data.frame of comparison metrics, one row per
statistic.}
\item{intensity_real}{pooled positive pixel intensities from real
samples (numeric vector).}
\item{intensity_synth}{same for synthetic samples.}
\item{per_sample}{data.frame of per-sample stats for both groups,
useful for plotting.}
}
\description{
Generates a batch of synthetic samples from \code{peak_params} and
compares their distributional summary statistics to those of real
samples. Use this to verify the synthetic generator's output
resembles real spectra before committing to a long pretraining run.
}
\details{
Five comparisons are reported, each with a Kolmogorov-Smirnov D
statistic and p-value (small p-value indicates the real and
synthetic distributions differ):

\enumerate{
\item \strong{pixel_intensity_pooled}: distribution of all positive
pixel values, pooled across samples. Tests whether the intensity
dynamic range matches.
\item \strong{total_signal}: per-sample sum of all pixels. Tests
overall signal magnitude.
\item \strong{mean_positive}: per-sample mean of positive pixels.
Tests typical peak amplitude scale.
\item \strong{sparsity}: per-sample fraction of zero pixels. Tests
whether the proportion of background-vs-signal region matches.
\item \strong{n_peaks}: number of detected peaks per sample (via
\code{\link[=pick_peak_centers]{pick_peak_centers()}}). Tests whether the peak density matches.
}
}
\seealso{
\code{\link[=plot_synthetic_quality]{plot_synthetic_quality()}} for a visual companion.
}

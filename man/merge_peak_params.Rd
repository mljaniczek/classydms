% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_catalog.R
\name{merge_peak_params}
\alias{merge_peak_params}
\title{Merge multiple peak_params objects into a single super-cohort}
\usage{
merge_peak_params(pp_list)
}
\arguments{
\item{pp_list}{Named list of \code{peak_params} objects. All must have
\code{sample_idx_raw}. Names become cohort labels.}
}
\value{
A single \code{peak_params}-shaped list with concatenated
\verb{_raw} vectors, renumbered \code{sample_idx_raw}, \code{sample_cohort}
giving per-sample cohort membership, and \code{cohort_names} (input
names).
}
\description{
Concatenates all \verb{_raw} vectors across the input peak_params
objects with globally-unique \code{sample_idx_raw}. Useful for building
a universal catalog from all cohorts jointly.
}
\details{
Each returned merged sample's original cohort is recorded in the
\code{sample_cohort} field, which is a character vector of length
\code{n_samples} giving the input list's name for each sample.
}

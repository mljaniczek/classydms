% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_simulation.R
\name{evaluate_biomarker_recovery}
\alias{evaluate_biomarker_recovery}
\title{Evaluate biomarker recovery from a saliency map}
\usage{
evaluate_biomarker_recovery(
  saliency_map,
  biomarkers,
  H = NULL,
  W = NULL,
  match_radius_rt = 8L,
  match_radius_cv = 2L,
  top_k = 10L
)
}
\arguments{
\item{saliency_map}{numeric \code{H} x \code{W} matrix in the same coordinate
frame as the biomarker locations (i.e. after any padding /
trimming used for the encoder input).}

\item{biomarkers}{ground-truth spec: tibble with at least \code{rt_loc}
and \code{cv_loc} columns (as fractions of image dimensions).}

\item{H, W}{dimensions of \code{saliency_map}. If \code{NULL}, taken from the
matrix.}

\item{match_radius_rt, match_radius_cv}{match radius in pixels.
Default 8 RT / 2 CV, matching the peak-detector's default
\code{min_sep_rt}, \code{min_sep_cv}.}

\item{top_k}{number of local maxima to extract for precision/recall.}
}
\value{
list(auc_per_pixel, precision_at_k, recall_at_k,
matched_biomarkers, missed_biomarkers, top_hotspots)
}
\description{
Two complementary metrics on the same saliency map and ground-truth
biomarker spec:
}
\details{
\enumerate{
\item \strong{Per-pixel AUC}: label each pixel \code{1} if within
\verb{(match_radius_rt, match_radius_cv)} of any biomarker center,
else \code{0}; score with the saliency value; report ROC AUC. This
is "does saliency track biomarker regions on average?"
\item \strong{Top-K precision / recall}: extract the top-K
non-max-suppressed local maxima of the saliency map; for each,
check whether it falls within the match radius of any biomarker
center. Precision = matched / K. Recall = unique biomarkers
matched / n_biomarkers. This is "does saliency's hotspot list
correspond to the biomarkers?"
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data.R
\name{estimate_peak_params}
\alias{estimate_peak_params}
\title{Estimate peak parameter distributions from real GC-DMS samples}
\usage{
estimate_peak_params(
  Z_list,
  Z_pretrim_list = NULL,
  time_axes = NULL,
  cv_axes = NULL,
  top_k = 150,
  frac_height = 0.25,
  min_sep_rt = 8,
  min_sep_cv = 2,
  verbose = TRUE
)
}
\arguments{
\item{Z_list}{List of trimmed, normalized intensity matrices (real data).}

\item{Z_pretrim_list}{Optional list of pre-dust-threshold matrices.}

\item{top_k}{Maximum peaks to detect per sample (default 150).}

\item{frac_height}{Fraction of peak height defining the width (default 0.25;
0.5 corresponds to FWHM).}

\item{min_sep_rt, min_sep_cv}{Minimum pixel separation between peaks.}

\item{verbose}{Whether to print diagnostic messages (default TRUE).}
}
\value{
A list of fitted distributions and raw observation vectors,
suitable for passing to \code{\link[=generate_one_synthetic]{generate_one_synthetic()}} or
\code{\link[=generate_synthetic_dataset]{generate_synthetic_dataset()}}. Fields \code{rt_loc_raw}, \code{cv_loc_raw},
\code{sigma_rt_raw}, \code{sigma_cv_raw}, and \code{intensity_raw} are parallel
per-peak vectors; \code{sample_idx_raw} gives the 1-based index of the
source sample for each peak (parallel to the other \verb{*_raw} vectors),
and \code{sample_names} is \code{names(Z_list)} for lookup. These enable
per-sample analyses (e.g. sample-level shift/clustering) without
changing existing callers.
}
\description{
Pools all samples (cases and controls together; no labels used) and
extracts empirical distributions of: number of peaks per sample, peak
locations (as fractions of image dimensions), peak widths (sigma_RT,
sigma_CV) measured at \code{frac_height} height and converted to Gaussian
sigmas, peak intensities, and background noise mean / SD. Widths are
fitted to log-normal distributions.
}
\details{
If \code{Z_pretrim_list} is provided (pre-dust-threshold matrices), peak
width measurement uses the pre-thresholded data as a fallback to
preserve peak flanks that the dust thresholding zeroed out.
}

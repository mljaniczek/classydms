% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{build_pretrained_classifier}
\alias{build_pretrained_classifier}
\title{Attach a classifier head to a pre-trained encoder}
\usage{
build_pretrained_classifier(
  encoder,
  num_classes = 2L,
  dropout_p = 0.3,
  freeze_backbone = TRUE,
  unfreeze_last_block = TRUE
)
}
\arguments{
\item{encoder}{A pre-trained \code{dms_encoder}.}

\item{num_classes}{Number of output classes (default 2).}

\item{dropout_p}{Dropout probability before the linear layer.}

\item{freeze_backbone}{If TRUE, encoder weights are not updated.}

\item{unfreeze_last_block}{If TRUE, the encoder's last residual block
(layer3) is set trainable even if \code{freeze_backbone = TRUE}.}
}
\value{
An instantiated torch module.
}
\description{
Deep-copies the encoder so each fold gets its own independent weights
(prevents cross-fold leakage when \code{unfreeze_last_block = TRUE}), wraps
it with adaptive average pooling and a dropout + linear classifier head.
}

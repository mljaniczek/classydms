% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{resample_to_time_grid}
\alias{resample_to_time_grid}
\title{Resample one sample onto a regular time grid via linear interpolation}
\usage{
resample_to_time_grid(sample, target_time, method = c("linear", "constant"))
}
\arguments{
\item{sample}{A sample list with \verb{$time} (numeric vector) and \verb{$Z}
(matrix with \code{nrow(Z) == length(time)}).}

\item{target_time}{Numeric vector of RT positions (seconds) — the
grid to interpolate onto. Typically from
\code{\link[=compute_common_time_grid]{compute_common_time_grid()}}.}

\item{method}{Interpolation method passed to \code{\link[stats:approxfun]{stats::approx()}}.
Default \code{"linear"}. Alternatives: \code{"constant"} (nearest-neighbor
step function).}
}
\value{
A sample list with updated \verb{$Z} (dims
\verb{length(target_time) x ncol(Z)}) and \verb{$time} set to \code{target_time}.
}
\description{
Interpolates each CV column of the sample's \verb{$Z} matrix from the
sample's own \verb{$time} vector onto a target regular grid. Values past
the sample's own maximum RT are filled with 0 (no signal). Returns
a new sample with \verb{$Z} on the target grid and \verb{$time} set to the
target grid.
}
\details{
Purpose: after resampling, row index \code{i} in every sample corresponds
to the same physical RT (\code{target_time[i]}), removing cross-sample
misalignment caused by variable per-row time deltas and different
RT endpoints. Downstream encoders / classifiers can then treat pixel
positions as physically meaningful and cross-sample-comparable.

\verb{$cv} is unchanged (CV grid is already consistent across samples on
the same instrument). Other list fields (\verb{$path}, \verb{$id}, etc.) are
preserved.
}
